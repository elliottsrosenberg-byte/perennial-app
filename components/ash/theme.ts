// Shared Ash visual constants. The signature Ash gradient is used across the
// floating FAB (AshContainer), the docked panel (AshPanel), the shared chat
// body (AshChatView) and the Home modal (AshHomeModal) — one definition so the
// mark reads identically everywhere. Token-only (see app/globals.css).

export const ASH_GRADIENT = "var(--gradient-ash)";
