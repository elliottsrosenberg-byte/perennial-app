{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | { read -r f; [ -n \"$f\" ] && node scripts/check-design-tokens.mjs \"$f\"; }",
            "statusMessage": "Checking design tokens…"
          }
        ]
      }
    ]
  }
}
